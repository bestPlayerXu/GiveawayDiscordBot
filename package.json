{
  "name": "giveaway",
  "version": "1.0.0",
  "description": "A giveaway bot with start in xyz feature.",
  "main": "index.js",
  "scripts": {
    "test": "node -r dotenv/config Main.js",
    "start": "node -r dotenv/config Main.js > logs/runtime.log 2> logs/error.log &",
    "stop": "kill $(head -1 logs/pid.log)"
  },
  "keywords": [
    "giveaway",
    "discord",
    "bot"
  ],
  "author": "bestPlayerXu",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0",
    "fs": "^0.0.1-security",
    "mariadb": "^2.5.2",
    "ping-a-monitor": "^5.0.0"
  }
}
